<%= form_with model: @inventory_food do |f| %>

  <% if @inventory_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inventory_food.errors.count, "error") %> prohibited this inventory from being saved:</h2>

      <ul>
      <% @inventory_food.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :food %><br />
    <%= f.collection_select :food, @foods, :id, :name, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.number_area :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>